# Database Configuration
# PostgreSQL 데이터베이스 연결 URL
# 예: postgresql://username:password@localhost:5432/sheet_manager
DATABASE_URL=postgresql://username:password@localhost:5432/sheet_manager

# Session Secret
# 세션 암호화를 위한 시크릿 키 (랜덤 문자열 사용 권장)
SESSION_SECRET=your-session-secret-key-here

# Server Port
# 서버가 실행될 포트 (기본값: 5000)
PORT=5000

# Node Environment
# development 또는 production
NODE_ENV=development

# Optional: Finnhub API Key
# 실시간 주가 데이터를 위한 Finnhub API 키 (선택사항)
# 설정하지 않으면 시뮬레이션된 가격 업데이트 사용
# FINNHUB_API_KEY=your-finnhub-api-key-here
